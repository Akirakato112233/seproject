# User app – services

API and client helpers used by the app.

- **api.ts** – Shop listing with filters (getShops), uses Config.API_URL.
- **apiClient.ts** – HTTP client helpers (e.g. authGet) for authenticated requests. Used by Home and other screens to talk to the backend.
